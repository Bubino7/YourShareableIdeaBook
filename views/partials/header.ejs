<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/content.css">
    <link rel="stylesheet" href="/styles/layout.css">
    <link rel="stylesheet" href="/images">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <!-- <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script> -->
    <title>Your Shareable IdeaBook</title>
</head>

<body>

    <nav class="navbar bg-success border-bottom border-black">
      <div class="container fluid">
        <!-- Navbar Logo -->
        <a class="navbar-brand" href="/">
          <span class="fw-bold">YsiB</span>
        </a>
        <!-- Navbar Date -->
        <% if (authorisationCheck === true) { %>
         <div class="d-none d-sm-block">
          Welcome dear <span class="fw-bold fst-italic"><%= locals.Username %></span>, it's <span class="fw-bold"><%= new Date().getDate() %> <%= new Date().toLocaleString('default', {month: 'long'});  %> <%= new Date().getFullYear();  %></span>
         </div>
        <% } else { %>
        <div class="fw-bold d-none d-sm-block">
          <%= new Date().getDate() %> <%= new Date().toLocaleString('default', {month: 'long'});  %> <%= new Date().getFullYear();  %>
        </div>
        <% } %>
        <!-- Navbar menu Button -->
        <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar" aria-controls="offcanvasNavbar" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <!-- Navbar menu -->
        <div class="offcanvas offcanvas-end bg-success text-sm-center" tabindex="-1" id="offcanvasNavbar" aria-labelledby="offcanvasNavbarLabel">
          <div class="offcanvas-header">
            <h5 class="offcanvas-title" id="offcanvasNavbarLabel">Your Shareable IdeaBook</h5>
            <button class="btn-close" type="button" data-bs-dismiss="offcanvas" aria-label="Close"></button>
          </div>
          <div class="offcanvas-body">
            <ul class="navbar-nav justify-content-end pe-3 gap-2">
              <li class="nav-item">
                <a href="/" class="nav-link active">Home</a>
              </li>
              <li class="nav-item">
                <a href="/home" class="nav-link">New Ideas</a>
              </li>
              <li class="nav-item">
                <% if (authorisationCheck === true) { %>
                 <a href="/profile">
                  <button class="btn btn-outline-light" type="button"><%= locals.Username %></button>
                 </a>
                <% } else {%>
                  <a href="/login">
                    <button class="btn btn-outline-light" type="button">Log in / Create an account</button>
                  </a>
                <% } %>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </nav>

<main>
